# Codex 安装包

这个包用于安装 Codex CLI。

## macOS

双击或终端运行：

```bash
./install-macos.command
```

## Windows

右键 `install-windows.ps1`，选择“使用 PowerShell 运行”。

## 下载源

默认使用：

```text
https://registry.npmmirror.com
```
